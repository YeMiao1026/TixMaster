# TixMaster Backend 環境變數範例
# 複製此檔案為 .env 並填入實際的值

# === 環境設定 ===
NODE_ENV=development
# 可選值: development, production, test
# 部署到 Railway/Render 時會自動設為 production

# === 資料庫 ===
DATABASE_URL=postgresql://username:password@host:5432/database_name
# Railway/Render 會自動提供 DATABASE_URL

# === 應用設定 ===
JWT_SECRET=your-secret-key-change-this-in-production
PORT=3000

# === Crash API 設定 ===
# ⚠️ 安全重要：生產環境請設為 false
# 只在需要測試監控系統時才設為 true
ENABLE_CRASH_API=false
# 開發環境建議設為 false，需要測試時再改為 true

# === Auth0 / OAuth 設定 ===
AUTH0_DOMAIN=your-auth0-domain.auth0.com
AUTH0_CLIENT_ID=your-auth0-client-id
AUTH0_CLIENT_SECRET=your-auth0-client-secret
AUTH0_CALLBACK_URL=http://localhost:3000/auth/callback
# 部署後請更新為實際的 callback URL

# === 其他可選設定 ===
# LOG_LEVEL=info
# SESSION_SECRET=your-session-secret
# FRONTEND_URL=http://localhost:5173