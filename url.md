# 情境一：給全端工程師 (Galin12341) —— 程式連線用

全端工程師只需要那串 完整連線字串 (Connection String)，讓他設定在環境變數即可。

請將以下資訊私訊傳給他：

🔧 資料庫連線資訊 (Render PostgreSQL)

請將專案根目錄 .env 檔案中的 DATABASE_URL 修改如下：

```sql
DATABASE_URL= ""
```

注意：這串網址包含密碼，請勿 commit 到 GitHub。

## 情境二：給 PM (YeMiao1026) 或 測試 (ww123) —— 視覺化管理工具

如果組員不會寫 SQL，或者想直接用滑鼠點點看資料，請他們安裝 DBeaver (免費、跨平台、最強大)。

你可以把下面這段教學直接貼到你們的群組：

📝 TixMaster 資料庫連線教學 (使用 DBeaver)
大家請安裝 DBeaver Community，安裝後依照以下步驟連線到我們的雲端資料庫：

- 開啟 DBeaver，點擊左上角的插頭圖示 [New Database Connection]。

- 選擇 PostgreSQL，點擊 Next。

- 在 Connection Settings 分頁填入以下資訊（不要複製錯格）：

- Host (主機):

- Port: 5432

- Database (資料庫):

- Username (使用者):

- Password (密碼):

- 點擊左下角的 [Test Connection]。

- 如果出現 Connected，代表成功！按 Finish。

現在你們可以在左側欄位展開 tixmasterdb -> Schemas -> public -> Tables，就可以看到所有資料表了 (Events, Users, Tickets...)。
